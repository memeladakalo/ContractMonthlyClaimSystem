@{
    // Default layout
    Layout = "_Layout";

    // Optional: Role-based layout selection
    // Uncomment and customize if you want different layouts for different roles
    /*
    if (User?.Identity?.IsAuthenticated == true)
    {
        if (User.IsInRole("HR"))
        {
            Layout = "_HRLayout";
        }
        else if (User.IsInRole("Manager"))
        {
            Layout = "_ManagerLayout";
        }
        else if (User.IsInRole("Lecturer"))
        {
            Layout = "_LecturerLayout";
        }
    }
    */
}
